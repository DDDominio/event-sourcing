name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: install PHP7
        run: sudo apt-get purge php-* && sudo apt-get install php-7.4

      - name: composer
        run: composer install